# furia-bff — TypeScript REST scaffold

Quick scaffold for a TypeScript + Express REST service.

Run locally:

```sh
npm install
npm run dev
```

Build for production:

```sh
npm run build
npm start
```
